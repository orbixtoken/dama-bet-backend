// src/constants/roles.js

// Centralização das funções (roles) do sistema
export const ROLES = {
  ADMIN: 'ADMIN',
  MASTER: 'MASTER',
  OPERADOR: 'OPERADOR',
  USUARIO: 'USUARIO'
};

// Opcional: se quiser uma lista pronta
export const ALL_ROLES = Object.values(ROLES);
